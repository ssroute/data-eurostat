name: ssroute_data_eurostat
description: Eurostat SeaRoute / MARNET maritime routing graph data (JSON tuples) for Dart. Works in VM, Flutter mobile, and Flutter web.
version: 0.2.0
homepage: https://github.com/ssroute/data-eurostat
repository: https://github.com/ssroute/data-eurostat
issue_tracker: https://github.com/ssroute/data-eurostat/issues

environment:
  sdk: ">=3.0.0 <4.0.0"

dependencies:
  # pure Dart; no Flutter dependency

